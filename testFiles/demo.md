# 测试 Character Counter 插件

<counter>
这是一个测试文本，包含汉字和英文字符。
This is a test text with Chinese and English characters.
本项目用于测试字符计数功能，当字符数达到200时会在下方添加分隔线。
The extension will add a separator line when character count reaches the goal.
</counter>

<counter>
简单测试
short test
</counter>

<counter>
这个计数器标签内的字符数量会逐渐增加，当达到配置的目标值（默认200）时会自动添加分隔线。分隔线包含时间戳和目标字符数信息，格式为：-------------------时间戳(yyyyMMddHHmm)|goalNumsEND--------------------
这是一个完整的测试用例，用于验证插件的各项功能是否正常工作。
</counter>