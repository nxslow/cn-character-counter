{
  "name": "char-counter",
  "displayName": "Character Counter",
  "description": "实时显示汉字数量并在计数器标签内达到目标时添加分隔线",
  "version": "1.0.0",
  "engines": {
    "vscode": "^1.107.1"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:char-counter.countCharacters",
    "onLanguage:plaintext",
    "onLanguage:markdown",
    "onLanguage:html"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "char-counter.countCharacters",
        "title": "Character Counter: Count Characters"
      }
    ],
    "configuration": {
      "title": "Character Counter",
      "properties": {
        "charCounter.goalNums": {
          "type": "number",
          "default": 30,
          "description": "Number of characters to trigger the separator line"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/node": "16.x",
    "typescript": "^4.9.4"
  }
}